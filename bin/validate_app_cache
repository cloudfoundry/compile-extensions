#!/usr/bin/env ruby

require 'fileutils'

APP_CACHE_DIR = ARGV.first 
CF_STACK_PATH = File.join(APP_CACHE_DIR, '.cf_stack')

unless File.exists?(CF_STACK_PATH) && File.read(CF_STACK_PATH) == ENV['CF_STACK']
  FileUtils.rm_rf APP_CACHE_DIR
end
